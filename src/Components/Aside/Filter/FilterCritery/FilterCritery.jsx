// import PropTypes from 'prop-types';

function FilterCritery() {
  // function inputRadioHandler(event) {
  //   if (event.target.checked) {
  //     dispatch(change(event.target.value));
  //   }
  // }

  // const filterItemId = `filter-${type}-${title.split(' ').join('-')}`;
  // const filterItemName = `filtration-${type}`;

  return (
    <div className="filter__item">
      {/* <input
        // checked={checked}
        // name={filterItemName}
        type="radio"
        className="filter__input_radio"
        // id={filterItemId}
        // value={title}
        // onChange={inputRadioHandler}
      />
      <label htmlFor={filterItemId} className="filter__label">
        <span className="filter__name">{title}</span>
        <span className="filter__amount">({amount})</span>
      </label> */}
    </div>
  );
}

// FilterCritery.propTypes = {};

export default FilterCritery;
