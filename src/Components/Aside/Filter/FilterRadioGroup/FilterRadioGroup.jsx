// import FilterCritery from '../FilterCritery/FilterCritery';

export default function FilterRadioGroup() {
  return (
    <fieldset className="filter__critery">
      {/* <h4 className="item-title">{title}</h4>
      <div className="filter__list">
        {criteria.map((criterion, index) => {
          return (
            <FilterCritery
              checked={criterion.title === activeCriterion}
              key={index}
              title={criterion.title}
              amount={criterion.amount}
              type={title}
              change={change}
            />
          );
        })}
      </div> */}
    </fieldset>
  );
}
